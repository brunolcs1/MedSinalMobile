<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/medsinalogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/medsinalogo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedSinal"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}"
            />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MedSinal"
            android:label="@string/medsinal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".BuscaSaudeActivity"
            android:exported="false" />

        <activity
            android:name=".SaudeConectadaActivity"
            android:exported="false" />

        <activity
            android:name=".ActivityHivAids"
            android:exported="false" />

        <activity
            android:name=".ActivitySifilis"
            android:exported="false" />

        <activity
            android:name=".ActivityHerpes"
            android:exported="false" />

        <activity
            android:name=".ActivityGonorreia"
            android:exported="false" />

        <activity
            android:name=".ActivityHpv"
            android:exported="false" />

        <activity
            android:name=".ActivityHepatite"
            android:exported="false" />

        <activity
            android:name=".ActivityTaci"
            android:exported="false" />

    </application>

</manifest>